apiVersion: v1
kind: Namespace
metadata:
  name: acom-offer-desk-pilot
  labels:
    app.kubernetes.io/part-of: acom-offer-desk
    acom.offering/environment: pilot
    # tier labels для NetworkPolicy (R-A3)
    acom.security/tier: mixed
