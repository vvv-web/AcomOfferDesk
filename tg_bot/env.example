# Скопируйте в tg_bot/.env (файл в .gitignore).
# Источник: tg_bot/src/app/core/config.py

# Токен от @BotFather
BOT_TOKEN=123456789:CHANGE_ME_BOT_TOKEN

# Внутри Docker-сети бот ходит в API через nginx gateway:
BACKEND_BASE_URL=http://gateway

# Публичный базовый URL API (тот же смысл, что PUBLIC_BACKEND_BASE_URL в backend/.env).
# После смены localtunnel/VPS — обновить и: docker compose up -d --force-recreate tg_bot
PUBLIC_BACKEND_BASE_URL=http://localhost:8080

# Таймаут HTTP к бэкенду (секунды)
REQUEST_TIMEOUT_SECONDS=5.0
